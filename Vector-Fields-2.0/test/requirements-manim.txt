# Minimum (PIL renderer + streaming)
websocket-client>=1.7.0
numpy>=1.24.0
Pillow>=10.0.0

# Full (ManimGL renderer)
# manimgl>=1.6.0
# Note: manimgl requires OpenGL, LaTeX (texlive), and scipy
# On macOS: brew install --cask mactex-no-gui && pip install manimgl
